Here are some test cases for the function that adds 1 to input:

Test case 1:
```
assert add_one(5) == 6, "Error: Should return 6"
```

Test case 2:
```
assert add_one(-1) == 0, "Error: Should return 0"
```

Test case 3:
```
assert add_one(100) == 101, "Error: Should return 101"
```

Test case 4:
```
assert add_one(0) == 1, "Error: Should return 1"
```

Test case 5:
```
assert add_one(-5) == -4, "Error: Should return -4"
```
