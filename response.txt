Sure! Here are some test cases you could use to test the function that adds 1 to the input:

```python
def test_add_one():
    assert add_one(1) == 2
    assert add_one(5) == 6
    assert add_one(10) == 11
    assert add_one(0) == 1
    assert add_one(-1) == 0
    assert add_one(-5) == -4
    assert add_one(100) == 101

test_add_one()
```

You can add these test cases to your test suite to verify that the function is working as expected.
