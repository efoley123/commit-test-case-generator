Here are some 'unittest' test cases based on the provided method signature:

```python
import unittest

def login_user(email, password):
    # implementation here
    pass

class TestLoginUser(unittest.TestCase):

    def test_successful_login(self):
        email = 'test@example.com'
        password = 'password123'
        self.assertTrue(login_user(email, password))

    def test_invalid_email(self):
        email = 'invalidemail'
        password = 'password123'
        self.assertFalse(login_user(email, password))

    def test_incorrect_password(self):
        email = 'test@example.com'
        password = 'wrongpassword'
        self.assertFalse(login_user(email, password))

if __name__ == '__main__':
    unittest.main()
```

Feel free to adjust the test cases as needed based on the actual implementation of the `login_user` function.
